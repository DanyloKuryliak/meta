NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key (admin operations, bootstrap-admin script)
# SUPABASE_SERVICE_ROLE_KEY=

# Apify token for fetching ads (used by Edge Functions)
# APIFY_TOKEN=

# Auth: email + 8-digit code. See docs/AUTH_SETUP.md.
# Admin synthetic email: when this email is entered, password field is shown instead of OTP.
# Set via bootstrap-admin.mjs. Must match exactly (e.g. admin@genesis.local).
NEXT_PUBLIC_ADMIN_EMAIL=admin@genesis.local
