NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key (admin operations, bootstrap-admin script)
# SUPABASE_SERVICE_ROLE_KEY=

# Apify token for fetching ads (used by Edge Functions)
# APIFY_TOKEN=

# Auth: email + 8-digit code. See docs/AUTH_SETUP.md.
# Admin email: this user gets admin rights (is_admin in user_profiles). Uses OTP like everyone else.
NEXT_PUBLIC_ADMIN_EMAIL=metacreatives.genesis@gmail.com
