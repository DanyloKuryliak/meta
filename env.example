NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key for server-side ingest/populate (bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=

# Meta Ads Library API (Graph API). Preferred for ingestion.
# Create app at https://developers.facebook.com and use Ads Library API token.
META_ACCESS_TOKEN=

# Apify: optional alternative to Meta API for fetching ads
# APIFY_TOKEN=
